on: push

jobs:
  npm-build:
    name: npm-build工作
    runs-on: ubuntu-latest
    steps:
    - name: 复制文件到虚拟机
      uses: actions/checkout@v4
    - name: 安装依赖
      run: npm install 
    - name: 构建项目
      run: npm run build
